class="img-fluid w-50" => helps to have an image inside a container then scale
te image to 50% 
d-flex=> display flex
d-sm-flex => exempting small screens from flex. only becomes flex when it hits the small breakpoint
class="lead"=> makes a paragraph stand out by increasing font size and lineheight
class="btn btn-primary btn-lg"=> creates a button blue in color which is a litte bit bigger that default
my-4=> space ubove an bd below
d-none d-sm-block=> hide element when you hit the small breakpoint